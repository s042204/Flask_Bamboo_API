version: "3.9"

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - BAMBOHR_API_URL=${BAMBOHR_API_URL}
      - BAMBOHR_API_TOKEN=${BAMBOHR_API_TOKEN}
